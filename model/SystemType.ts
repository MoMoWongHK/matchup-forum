export enum LANGUAGE {
  HK = "HK",
  EN = "EN",
}

export enum GENDER {
  M = "M",
  F = "F",
  U = "U",
}

export enum INDUSTRY {
  DEFAULT,
}

export enum LOCATION {
  DEFAULT,
}

export interface DateObj {
  seconds: number;
  milliseconds: number;
}
