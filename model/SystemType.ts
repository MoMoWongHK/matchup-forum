export enum LANGUAGE {
  HK = "HK",
  EN = "EN",
}

export enum GENDER {
  M = "M",
  F = "F",
  U = "U",
}

export enum INDUSTRY {}

export enum LOCATION {}

export interface DateObj {
  seconds: number;
  milliseconds: number;
}
